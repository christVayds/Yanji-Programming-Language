
Yanji Programming Language is a typed, low-level and high-level programming Language.
Designed to be fast, modern and beginer-friendly.

self.symbol_table = {
    'name': {
        'ptr': pointer-of-the-variable,         # ptr of the variable
        'type': int,                            # type of the variable
        'text_bytes': textbytes(),              # textbytes only for str
        'mutable': true,                        # if mutable
    }
}

Bugs and errors:
    1. Printing - check later

TO-DOs
    1. Exceptions and error handling
    2. if statements
    3. for loops 
    4. while loops 
    5. functions
    6. structs
    7. enum
    8. logical
    9. ==, !=, >, <, >=, <=
    10. switch
    11. do-while loops
    12. re-assigning variables              # complete
    13. complete the data types of int and double

Scopes
    1. local 
    2. global
    3. block scopes 
    4. function scopes 

datatypes:
    int
    int8
    int64

    uint
    uint8
    uint64

    double
    udouble
    double64
    udouble8

    str
    char
    bool

    struct
    enum
    arrays 
    lists

int age = 20                # declared
age = 30                    # error - immutable variable cannot be change
str msg = "hello world"     # string
int8 sum;                   # undeclared value
mut int x = 100             # mutable variable
x = 30                      # only mutable variable can be change

IO:
    write name              # only print string
    read name               # get input from user

# ARRAYS
int arr[10] = {}        # array of type int
int tups[10] = ()       # tuple of type int
# especial array
(str name, int age, double grade) student[10] = {
    {"christian", 20, 80.5}
}

# STRUCT
struct Student(
    str name
    int age
    double grade
)

# create new struct
struct student1 = new Student("christian", 20, 80.5)

# access struct
student1.name

# ENUM
enum State(
    DAY,
    NIGHT,
    MIDNIGHT 
)

# IF STATEMENT
if(age > 20){
    age = age + 1
} ifelse (age < 19){
    age--
} else {
    age = 20
}

# FOR LOOPS
for(int i=0;i<age;i++){
    age++
}

# FOREACH LOOPS
foreach(int num in numbers){

}

# WHILE LOOPS
while(true){
    # do something
}

# FUNCTION
func int main(){
    int num = 20
    return 0;
}

func void sayHello(str msg){

}

# OOP

-> create a struct
struct Student (
    str name
    int age
    double grade

    # enum functions
    func void Copy()
)

# function statement
func void Student::Copy(){

}